import LineHealthBar from "./LineHealthBar";

export default function GenerationLineCard() {
  return (
    <section className="card generation-line-card">
      <LineHealthBar />
    </section>
  );
}
