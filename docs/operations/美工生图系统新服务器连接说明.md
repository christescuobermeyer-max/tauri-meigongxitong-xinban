# 美工生图系统新服务器连接说明

> 生效日期：2026-08-01  
> 适用项目：呈尚策划美工生图系统 PRO、Rust/Axum 云端网关  
> 当前状态：生产域名已切换到新服务器，旧服务器网关已停止并禁用

## 1. 生产连接信息

| 项目 | 当前生产值 |
|---|---|
| 公网业务地址 | `https://gw.hbcsch.pw` |
| DNS A 记录 | `156.225.23.248`，TTL 600 秒 |
| SSH | `admin@156.225.23.248:22` |
| 主机名 | `ser621695285778` |
| 本机已验证私钥 | `C:/Users/Administrator/.ssh/id_ed25519` |
| ED25519 主机指纹 | `SHA256:nf3gj+eZSpqYwU4DYY2RJ+BKxDdcS/YEBW3wIq+lZiY` |
| 项目源码 | `/opt/csgh-image-studio` |
| 网关运行目录 | `/opt/csgh-gateway` |
| 网关服务 | `csgh-backend-gateway` |
| 网关本机监听 | `127.0.0.1:8787` |
| Caddy 配置 | `/etc/caddy/Caddyfile` |
| 网关密钥文件 | `/opt/csgh-gateway/secrets/gateway.env`，只记录路径，不读取或复制内容 |
| 状态目录 | `/opt/csgh-gateway/state` |

客户端始终使用域名，不要把裸 IP 写进应用：

```dotenv
VITE_BACKEND_GATEWAY_URL=https://gw.hbcsch.pw
```

新服务器的 HTTPS 证书签发给 `gw.hbcsch.pw`。直接使用 `https://156.225.23.248` 会遇到证书主机名不匹配，也会绕过稳定的域名切换和回滚机制。

## 2. 当前切换状态

- 阿里云权威 DNS、Cloudflare DNS 和 Google DNS 已返回新 IP。
- 新机 `csgh-backend-gateway`、`caddy` 均为 `active/enabled`。
- 新机已使用生产 Supabase 配置完成 3 次真实生图，结果 3/3 成功；临时账号及关联记录已删除。
- 旧机 `csgh-backend-gateway` 为 `inactive/disabled`，旧机本地 8787 已关闭。
- DNS 缓存期内，旧机 Caddy 的美工生图默认路由会转发到新机；旧配置备份为 `/etc/caddy/Caddyfile.pre-csgh-new-server-20260801`。
- 旧机合同 API、OCR 和自动出餐服务未随本次美工网关切换停用。

部分局域网 DNS 可能继续缓存旧 IP 最多约 600 秒。此时客户端虽然先建立到旧 Caddy 的连接，业务仍由新网关处理；缓存过期后会直接连接新 IP。

## 3. SSH 登录

本迁移工作站使用 Git 忽略的 `.env.ssh.local` 保存主机、用户、端口和私钥路径。当前 Windows 工作站可直接执行：

```powershell
ssh -o BatchMode=yes \
  -o StrictHostKeyChecking=yes \
  -i C:/Users/Administrator/.ssh/id_ed25519 \
  -p 22 admin@156.225.23.248
```

其他电脑必须使用该电脑自己的已授权私钥，不要复制本机私钥文件。首次连接前核对服务器 ED25519 指纹与第 1 节一致。

连接后验证：

```bash
hostname
id
sudo -n true
```

预期主机名为 `ser621695285778`，当前用户为 `admin`，`sudo -n true` 返回 0。不要恢复 root 密码 SSH，也不要把私钥或服务器密码写入仓库。

### 3.1 公网 IP 或 VPN 变化后无法连接

如果 SSH 因本机公网 IP、运营商线路或 VPN 出口变化而超时，应保持 VNC 控制台打开。VNC 不一定能正确粘贴多行脚本，因此下面每个代码框都只有一条命令：必须按顺序逐条复制、粘贴并按回车，不要一次复制多个代码框。

第一步，确认 `admin` 公钥存在：

```bash
sudo test -s /home/admin/.ssh/authorized_keys && echo ADMIN_KEY_OK || echo ADMIN_KEY_MISSING
```

必须看到 `ADMIN_KEY_OK`。如果显示 `ADMIN_KEY_MISSING`，立即停止，不要执行后面的命令。

第二步，修正公钥目录权限：

```bash
sudo chown -R admin:admin /home/admin/.ssh && sudo chmod 700 /home/admin/.ssh && sudo chmod 600 /home/admin/.ssh/authorized_keys && echo ADMIN_KEY_PERMISSIONS_OK
```

第三步，写入只允许私钥登录的 SSH 配置。下面虽然可能在文档中自动换行显示，但它是一条完整命令，只复制这一个代码框：

```bash
printf '%s\n' 'PubkeyAuthentication yes' 'PasswordAuthentication no' 'KbdInteractiveAuthentication no' 'PermitRootLogin no' 'MaxAuthTries 3' 'LoginGraceTime 30' 'MaxStartups 30:30:100' 'TCPKeepAlive yes' 'ClientAliveInterval 60' 'ClientAliveCountMax 3' 'UseDNS no' | sudo tee /etc/ssh/sshd_config.d/00-csgh-key-only.conf >/dev/null
```

第四步，在重启前验证配置语法：

```bash
sudo sshd -t && echo SSH_CONFIG_OK
```

必须看到 `SSH_CONFIG_OK`。如果出现错误，立即停止并保持 VNC 打开。

第五步，允许任意公网 IP 或 VPN 出口访问 22 端口：

```bash
sudo ufw allow 22/tcp comment 'SSH key-only access' && sudo ufw reload && echo UFW_SSH_OK
```

第六步，启用并重启 SSH：

```bash
sudo systemctl enable ssh && sudo systemctl restart ssh && sudo systemctl is-active ssh
```

最后一行必须显示 `active`。然后执行最终检查：

```bash
sudo sshd -T | grep -E '^(pubkeyauthentication|passwordauthentication|kbdinteractiveauthentication|permitrootlogin|maxstartups|clientaliveinterval|clientalivecountmax)'
```

该配置允许任意公网 IP 或 VPN 出口访问 22 端口，但关闭 SSH 密码登录和 root 登录，只允许已写入 `/home/admin/.ssh/authorized_keys` 的私钥。全部完成后，先从另一终端建立新的 SSH 会话并验证成功，再关闭 VNC。

## 4. 客户端连接与构建

生产安装包构建：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/build-employee-installer.ps1 `
  -GatewayUrl "https://gw.hbcsch.pw"
```

前端 `VITE_SUPABASE_URL` 必须与生产网关使用同一个 Supabase 项目。若登录成功但网关返回 401，应先比较 Supabase 项目标识，不要轮换网关密钥或降低认证要求。本机存在其他项目的 `.env.local` 时，不得拿它验证生产网关。

## 5. 日常运维

```bash
ssh -i C:/Users/Administrator/.ssh/id_ed25519 -p 22 admin@156.225.23.248
cd /opt/csgh-image-studio

sudo systemctl status csgh-backend-gateway --no-pager -l
sudo journalctl -u csgh-backend-gateway -n 200 --no-pager
sudo systemctl status caddy --no-pager -l
curl -fsS http://127.0.0.1:8787/health
curl -fsS https://gw.hbcsch.pw/health
```

部署代码前先检查服务器工作树，不得清理未提交文件：

```bash
sudo -u csgh git -C /opt/csgh-image-studio status --short
```

只有确认待发布源码、Lockfile 和运行时边界后，才执行项目既有更新脚本或构建流程。更新后必须检查本机健康、公网健康、日志和一次真实生图。

## 6. 验收标准

```bash
curl -fsS https://gw.hbcsch.pw/health
```

必须同时满足：

1. 公共 DNS 仅返回 `156.225.23.248`。
2. HTTPS 证书域名为 `gw.hbcsch.pw`，验证结果为 0。
3. 新网关和 Caddy 均为 `active/enabled`。
4. 旧网关保持 `inactive/disabled`。
5. 登录、真实生图、OSS 云端历史和 Supabase 历史均正常。

### 6.1 当前 Windows 工作站 DNS 覆盖

当前路由器 DNS 在切换后仍短暂返回旧 IP。为保证本工作站立即直连新机，已在 `C:\Windows\System32\drivers\etc\hosts` 添加：

```text
156.225.23.248 gw.hbcsch.pw
```

实测 `curl` 的远端 IP 已为 `156.225.23.248`，HTTPS 状态 200。待路由器 DNS 稳定返回新 IP 后，可以删除这条 hosts 记录并执行 `Clear-DnsClientCache`；删除前先用公共 DNS 和本机 `nslookup` 双重确认。

## 7. 回滚边界

旧服务器仅作为观察期回滚源，不再作为美工生产网关。只有新机发生无法快速修复的生产故障时，才按 `docs/operations/云服务器全量迁移计划.md` 恢复旧 Caddyfile、启动旧网关并把 DNS A 记录改回旧 IP。禁止新旧网关同时直接承接生产流量。
