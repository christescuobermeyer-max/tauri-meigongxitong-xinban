# 线路6/7生产保护与重复扣费修复计划

## 目标

在不继续触发线路6/7收费请求的前提下，阻止不确定结果的重复重试，并为后续扣费核对增加可追踪信息。

## 阶段

- [x] 阶段1：暂停线路6/7并记录当前生产状态
- [x] 阶段2：实现最小代码保护（线路6显式 base64、请求追踪、模糊结果不重试）
- [ ] 阶段3：本地测试、编译并部署网关
- [ ] 阶段4：生产健康检查、暂停状态和日志验收
- [ ] 阶段5：整理交付记录

## 约束

- 不读取或输出 `gateway.env`、API Key、完整签名 URL。
- 不发起收费生图测试。
- 不回退工作区其他用户改动。
- 未确认上游支持前，不切换线路7模型。

## 当前判断

- 线路6主要存在额度/网络失败；线路7主要存在连接超时和响应体读取失败。
- 线路2/3/4共享失败后的 fallback 会增加线路6/7压力，但不是线路6/7全部失败的唯一原因。
- 当前网关对可能已被上游接受的请求继续重试，存在重复扣费风险。

## 状态

**当前处于阶段3**：代码保护已完成，本地网关完整测试通过；生产部署因 SSH 在密钥交换前被服务器主动关闭而暂缓，线路6/7暂停保护仍保持。

## 错误记录

- 生产暂停状态已写入 `/opt/csgh-gateway/state/paused-lines.json`。
- 已保存生产状态备份：`paused-lines.json.bak-protection-20260921`。
- 本地网关测试：124 passed，0 failed，2 ignored。
- 线路6/7契约测试及线路2契约测试通过。
- 开发机首次编译因 VS Insiders 缺少 `vcruntime.h` 失败，切换 VS 2022 BuildTools 后通过。
- 2026-09-21 继续部署时，公网健康检查正常，但 SSH/`ssh-keyscan` 均在 banner 前被远端关闭；未执行生产源码安装、编译或重启。
