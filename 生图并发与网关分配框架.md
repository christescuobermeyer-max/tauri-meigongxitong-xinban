# 多用户多线路生图并发与网关分配框架

> 一份可以拿到其他项目直接复用的并发调度、上游路由、健康监测、失败重试设计文档。
>
> 实战来自呈尚策划 · 美工生图系统 PRO。Tauri 2 桌面端（React 18 + TypeScript）+ Rust（axum）云端网关。
> 上游有 6 条互为备份的 AI 生图服务（wlai / yunwu / vectorengine / pockgo / apimart / manxiaobai），
> 每条上游成本不同、并发上限不同、稳定性不同。
>
> **目标读者**：在做"多用户 + 多上游 + 需要排队 + 需要兜底 + 需要自动重试"的工程师，
> 想抄走一套久经实战检验的并发调度框架。

---

## 目录

- [一、整体架构](#一整体架构)
- [二、关键概念](#二关键概念)
- [三、前端层：Slot 模型 + 账号级软上限](#三前端层slot-模型--账号级软上限)
- [四、网关层：限流器 / FIFO 队列 / 健康监测](#四网关层限流器--fifo-队列--健康监测)
- [五、上游适配层：多线路抽象](#五上游适配层多线路抽象)
- [六、完整请求生命周期](#六完整请求生命周期)
- [七、配置项总览](#七配置项总览)
- [八、关键测试覆盖](#八关键测试覆盖)
- [九、设计取舍与踩坑记录](#九设计取舍与踩坑记录)
- [十、如何把这套框架移植到新项目](#十如何把这套框架移植到新项目)

---

## 一、整体架构

### 1.1 三层结构

```
   ┌──────────────────────────────────────────────────────────────────┐
   │                 桌面端 / Web 前端（React + TS）                   │
   │                                                                  │
   │   - 5 个店铺 slot × N 个工作区（三件套/单图/全店/...11 个工作区） │
   │   - 每个 slot 自维护 busy 标志（防止重复提交同一 slot）          │
   │   - 账号级软上限：同一账号最多 10 个任务同时进行中（防误触）     │
   │   - 永远以 api_line="auto" 发起请求，不让用户挑线路               │
   └──────────────────────────┬───────────────────────────────────────┘
                              │  HTTPS POST /api/generate-image
                              │  Bearer <Supabase access_token>
                              ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │                Backend Gateway（Rust + axum）                     │
   │                                                                  │
   │   FIFO 队列 + 三级并发限制 + 健康监测 + auto routing + 自动重试  │
   │                                                                  │
   │   ① 校验 Supabase access_token、确认账号 is_active                │
   │   ② 进入 FIFO 队列等待 slot                                       │
   │   ③ 选最优健康线路（line1=fallback only）                         │
   │   ④ 调用上游，失败自动 retry 切其它健康线路（含 5 路兜底）        │
   │   ⑤ 把延迟/成功/失败写进线路健康环形缓冲                          │
   │   ⑥ 返回 base64 给客户端                                          │
   └──────────────────────────┬───────────────────────────────────────┘
                              │  HTTPS（带各家自有 API Key）
                              ▼
   ┌──────────────────────────────────────────────────────────────────┐
   │                6 条上游 AI 生图服务（互为备份）                   │
   │                                                                  │
   │   line1 wlai          line2 yunwu        line3 vectorengine       │
   │   line4 pockgo        line5 apimart      line6 manxiaobai         │
   └──────────────────────────────────────────────────────────────────┘
```

### 1.2 为什么要做这套调度

| 问题 | 这套框架的解 |
|---|---|
| 上游可能临时挂掉 | 健康环形缓冲，5 次连续失败标 Red，auto 路由跳过 |
| 上游响应慢（10-100s 不等） | reqwest 客户端 350s 超时，超时算失败 |
| 单上游并发有限 | 每条线路单独配并发，全局再设上限 |
| 单用户可能误触刷爆服务 | 三级限制：全局 21 / 线路 1-5 / 账号 3 |
| 高成本线路想少用 | "Fallback line" 二级路由，只在其它线路 ≤1 条可用时启用 |
| 失败请求让用户重新排队体验差 | 失败自动切到没试过的健康线路，最多 6 次重试 |
| Red 线路怎么恢复 | 5 min 无样本自动转 Unknown，会被一次"探测"请求重新评估 |
| 同账号刷队列把别人卡死 | FIFO 跳头部：队伍里第一个还在自己 user_limit 内的请求才能 acquire |

---

## 二、关键概念

### 2.1 三级并发限制

```
            ┌───────────────────────────────────┐
            │  Global  21  ←  网关进程同时跑 21 │
            └───────────────────────────────────┘
                          │
   ┌──────────────────────┴───────────────────────┐
   ▼                                              ▼
┌────────────────────────────┐  ┌────────────────────────────┐
│  Per-line (按上游线路设)   │  │  Per-user (按账号设)        │
│  line1=1  line2=4  ...      │  │  user_limit=3              │
│  line5=5  line6=4           │  │  一个账号最多 3 个 in-flight│
└────────────────────────────┘  └────────────────────────────┘
```

三个限制是 **AND** 关系：一次 acquire 要同时满足全局有空位、目标线路有空位、当前账号没满 user_limit。

### 2.2 健康度环形缓冲

每条线路一个长度为 5 的环形缓冲，每次上游响应完成（成功/失败）压入一条样本：

```
样本 = { 发生时间 occurred_at, 延迟 latency_ms, 是否成功 success }
```

根据缓冲计算状态：

| 状态 | 触发条件 | 路由行为 |
|---|---|---|
| Green | failure_count < 5 且 中位延迟 < 150_000ms | 正常候选 |
| Yellow | failure_count < 5 且 中位延迟 ≥ 150_000ms | 正常候选（仅 UI 警示） |
| Red | failure_count >= 5（最近 5 次全失败） | **被路由排除** |
| Unknown | 缓冲为空 或 最近样本距今 > 5 min | 正常候选（作为"探测"） |

### 2.3 Fallback Line

把成本最高的 line1 设为 fallback：

- 只要还有 ≥ 2 条非 line1 线路健康可用，**永远不选 line1**
- 非 line1 候选 ≤ 1 时才让 line1 进入候选池"补位"

### 2.4 Auto Retry with `tried_lines`

一个用户请求在网关层最多尝试 6 次（每次切到没试过的健康线路）。
每次失败前，把刚试过的线路写进 `tried_lines: HashSet<String>`，下次 acquire 时排除掉。

```
请求进入 → acquire → line2 → 失败 → tried={line2} →
            acquire (排除 line2) → line5 → 失败 → tried={line2,line5} →
            acquire (排除 line2,5) → line3 → 成功 → 返回
```

### 2.5 FIFO + 账号级公平

FIFO 队列保证"先提交先服务"，但允许"跳过"：如果队首请求所属用户已达 user_limit，跳过他去看后面的请求，让别的用户先用。

```
queue: [A1, A2, A3, A4(skip), A5(skip), B1, A6(skip), C1]
              ↑ A 用户已经 3 in-flight，A4/A5/A6 暂时不能 acquire
              B、C 可以正常拿 slot，不被 A 卡住
```

---

## 三、前端层：Slot 模型 + 账号级软上限

### 3.1 工作区切片成多个独立 Slot

每个"工作区"（如"三件套"、"制作 1 张设计图"、"图片墙"...）切成 **5 个店铺 slot**。每个 slot 是这个 hook 的一个**独立实例**：

```ts
// useGenerationWorkspace.ts 摘要
const threePieceSlot1 = useThreePieceWorkspace({ ... });
const threePieceSlot2 = useThreePieceWorkspace({ ... });
...
const threePieceSlot5 = useThreePieceWorkspace({ ... });
const threePieceSlots = [
  threePieceSlot1, threePieceSlot2, threePieceSlot3, threePieceSlot4, threePieceSlot5,
] as const;
```

每个 slot 有独立的 state：`shopName`、`images`、`product`（生成结果）、`busy` 等。

```
UI:  店铺 1  店铺 2  店铺 3  店铺 4  店铺 5
     ┌──┐   ┌──┐   ┌──┐   ┌──┐   ┌──┐
     │A │   │B │   │  │   │  │   │  │
     └──┘   └──┘   └──┘   └──┘   └──┘
     busy   busy   idle   idle   idle
```

用户切 tab 就是切换显示哪个 slot 的 state。**同时最多 5 个生图任务在并行**（同一工作区内）。

### 3.2 防止同 slot 重复提交：`pi.busy`

按钮的 `disabled` 条件：

```tsx
submitDisabled={pi.busy}
```

`pi.busy` 是该 slot 内 product/avatar/... 项的状态。处于 `queued`、`running` 时为 true。

**这一条防止用户在结果还没出来前对**同一个店铺 slot**重复点击**。

### 3.3 账号级软上限：10 并发

跨所有工作区、所有 slot 汇总当前 in-flight 任务数，超过 10 就把所有提交按钮变灰，并显示提示：

```ts
const FRONTEND_GENERATION_USER_LIMIT = 10;

const activeGenerationTaskCount = countAllBusySlots();
const generationCapacityFull = activeGenerationTaskCount >= 10;
```

```tsx
<WorkspacePage globalBusy={workspace.generationCapacityFull} />
```

**为什么是 10 而不是更低**：网关本身已经有 `user_limit=3` 的硬限制（同一用户最多 3 个并发跑到上游），前端 10 个是软缓冲：

- 用户可以连续点 5-10 次，全部进入网关队列
- 网关同时只跑 3 个，剩 7 个排队
- 等其中一个完成，下一个 ticket 自动 acquire 启动
- 用户视角无需手动重新提交、不会卡死

设成 10 是经验值——再大会导致服务端排队过深，再小又起不到"软缓冲"作用。

### 3.4 前端永远不让用户挑线路

整个前端代码里 `api_line` 字段统一硬编码 `"auto"`：

```ts
const req = {
  prompt,
  size: "1024x1536",
  product_images: [...],
  api_line: "auto",  // ← 永远 auto
};
```

UI 上虽然有"线路选择器"显示当前用了哪条线路，**但那是 server 返回的事后结果**，不是用户的输入。这样设计的好处：

1. 用户不需要懂上游差异
2. 路由决策完全由服务端 health-aware 调度，可以随上游故障实时调整
3. 老客户端发出去的 api_line 哪怕被 hack 改成具体线路，服务端代码也会把它当 auto 处理

---

## 四、网关层：限流器 / FIFO 队列 / 健康监测

### 4.1 三大核心模块

```
        ┌──────────────────────────────────────┐
        │       HTTP handler (axum)             │
        │   /api/generate-image                 │
        └───────────────┬──────────────────────┘
                        │  verify_access_token
                        ▼
        ┌──────────────────────────────────────┐
        │    GatewayGenerationQueue             │
        │    (FIFO 队列 + 用户级公平调度)        │
        └───────────────┬──────────────────────┘
                        │  acquire permit
                        ▼
        ┌──────────────────────────────────────┐
        │       GatewayLimiter                  │
        │       (全局/线路并发 + 健康过滤 + 排序)│
        └───────────────┬──────────────────────┘
                        │  selected line
                        ▼
        ┌──────────────────────────────────────┐
        │       Upstream Call (api.rs)          │
        │       (调用 line1-line6 对应 SDK)     │
        └───────────────┬──────────────────────┘
                        │
                        ▼
                LineHealthRegistry.record(line, latency, success)
```

### 4.2 GatewayLimiter（限流器）

**职责**：维护"全局已用槽位"、"每条线路已用槽位"，提供查询 + 申请 + 释放接口。

```rust
// src-tauri/src/gateway_limiter.rs 摘要
pub struct GatewayLimiter {
    global_limit: usize,                      // 全局上限 21
    line_limits: HashMap<&'static str, usize>,// 每线路上限 {line1:1, line2:4, ...}
    active_global: usize,                     // 当前全局在跑数
    active_by_line: HashMap<String, usize>,   // 每线路在跑数
}

impl GatewayLimiter {
    pub fn try_acquire_auto_excluding(
        &mut self,
        size: &str,
        health: &LineHealthSnapshot,
        exclude: &HashSet<String>,  // 重试时用：排除已经试过的线路
    ) -> AcquireDecision;

    pub fn release(&mut self, line: &str);
}
```

#### 4.2.1 候选选择算法（核心！）

两级筛选 + 排序：

```rust
pub fn select_generation_line_excluding(...) -> Option<&'static str> {
    // 第一轮：只看非 line1 候选
    let primary = collect_auto_candidates(..., include_fallback=false);
    if primary.len() >= FALLBACK_PROMOTE_THRESHOLD {  // 2
        return pick_best(primary);
    }

    // 第二轮：非 line1 候选不足 2 个，把 line1 也加入候选
    let with_fallback = collect_auto_candidates(..., include_fallback=true);
    pick_best(with_fallback)
}

fn collect_auto_candidates(size, health, exclude, include_fallback) -> Vec<...> {
    AUTO_GENERATION_LINES.iter()
        .filter(|line| include_fallback || *line != "line1")  // fallback 控制
        .filter(|line| !exclude.contains(*line))              // 重试排除
        .filter(|line| supports_generation_size(line, size))  // 尺寸兼容
        .filter_map(|line| {
            // 线路 limit > 0
            // active < limit
            // health 不是 Red
            // 通过则收集 (line, health_rank, active, latency, order)
        })
        .collect()
}

fn pick_best(mut candidates) -> Option<&'static str> {
    candidates.sort_by_key(|(_, hr, active, latency, order)| {
        (active, hr, latency, order)  // 排序键
    });
    candidates.first().map(|c| c.0)
}
```

**排序键的语义（从主到次）**：

1. **active**：当前已用槽位少的优先 → 把负载摊开
2. **health_rank**：Green=0 < Unknown=1 < Yellow=2 < Red=3，越健康越优先
3. **latency_ms**：中位延迟低的优先（同等其它条件下选快的）
4. **auto_line_order**：所有指标都打平时按预设顺序 `[line2,line3,line4,line5,line6,line1]` 选

#### 4.2.2 Fallback Line 设计

```rust
const FALLBACK_LINE: &str = "line1";
const FALLBACK_PROMOTE_THRESHOLD: usize = 2;
```

意图：line1 上游 wlai 单张成本极高，平时不用、关键时刻兜底。

- 非 line1 候选 ≥ 2 时：line1 完全不参与排序 → 永远不会被选中
- 非 line1 候选 = 1 时：line1 加入与那 1 条线路一起排序 → 多数情况那条线路还是赢，但 active 满了会让 line1 接住
- 非 line1 候选 = 0 时：line1 是唯一候选 → 必然被选

**关键实战教训**：旧版本没有 Fallback Line 设计，line1 因为响应快（latency 排第一），日常占了 17% 流量；改成 fallback 后，**生产监控 10 分钟里 line1 流量降到 0%**。

### 4.3 GatewayGenerationQueue（FIFO 队列）

**职责**：当 limiter 没空位时挂起请求；当容量释放时按 FIFO 顺序唤醒下一个能 acquire 的请求。

```rust
// src-tauri/src/gateway_queue.rs 摘要
pub struct GatewayGenerationQueue {
    inner: Mutex<QueueState>,
    notify: Notify,
    line_health: Arc<LineHealthRegistry>,
}

struct QueueState {
    limiter: GatewayLimiter,
    user_limit: usize,                   // 每账号并发上限 3
    active_by_user: HashMap<String, usize>,  // 每账号在跑数
    next_ticket: u64,
    waiting: VecDeque<QueueTicket>,
}

struct QueueTicket {
    id: u64,
    user_id: String,
    request: QueueRequest,
}

enum QueueRequest {
    Auto { size: String, exclude: HashSet<String> },  // exclude 给 retry 用
    Line { line: String },                            // 显式指定线路（manual）
}
```

#### 4.3.1 acquire 流程

```rust
async fn acquire(self, request, user_id) -> Result<Permit, String> {
    // ① 提前判断：这个 request 有没有可能在未来某个时刻被满足
    //    例如 size 不支持任何线路、或所有线路已被禁用，直接报错
    if !request.can_ever_run(...) { return Err(...); }

    // ② 入队
    let ticket_id = next_ticket++;
    waiting.push_back(QueueTicket { id, user_id, request });

    loop {
        let notified = self.notify.notified();
        notified.enable();

        // ③ 试着拿到 slot（队列里"我"是否轮到了？且我自己 user_limit 内？）
        match try_acquire_front(ticket_id) {
            Ok(Some(line)) => return Ok(Permit { line, user_id }),
            Ok(None) => {},                // 还没轮到，等
            Err(message) => return Err(message),
        }

        // ④ 等待"有 slot 释放"的信号
        notified.await;
    }
}
```

#### 4.3.2 try_acquire_front（核心调度）

```rust
async fn try_acquire_front(&self, ticket_id) -> Result<Option<String>, String> {
    let mut state = self.inner.lock();
    let health = self.line_health.snapshot();  // ← 每次唤醒都重读 health

    // 找到队列中第一个能 acquire 的 ticket
    let selected = waiting.iter()
        .find(|t| t.can_acquire(&limiter, &health, user_limit, &active_by_user))
        .map(|t| t.id);

    // 如果不是"我"，说明前面有别人能用 slot，我继续等
    if selected != Some(ticket_id) {
        return Ok(None);
    }

    // 真正 acquire
    let line = limiter.try_acquire_auto_excluding(size, &health, &exclude).line;
    // 更新 active_by_user
    *active_by_user.entry(user_id).or_insert(0) += 1;
    self.notify.notify_waiters();  // 唤醒其他等待者重新评估
    Ok(Some(line))
}

// can_acquire 判定
fn can_acquire(...) -> bool {
    if active_by_user[user_id] >= user_limit { return false; }  // ← 用户级 fair
    // 全局有空位 && 选得到一条非 Red 非满线路
    limiter.has_global_capacity() && limiter.select_generation_line(...).is_some()
}
```

**关键点**：`find(...)` 而不是 `front()`。
意味着 FIFO 是"软"的：**队首如果是个已达 user_limit 的用户的 ticket，会被跳过，让后面的别人先用**。

这就是"账号公平性"——保证一个用户排再多请求，也不会饿死别人。

#### 4.3.3 Permit 释放

```rust
pub struct QueuedGenerationPermit {
    queue: Arc<GatewayGenerationQueue>,
    line: String,
    user_id: String,
}

impl Drop for QueuedGenerationPermit {
    fn drop(&mut self) {
        self.queue.release(&self.line, &self.user_id);
        // release 内部：
        //   - limiter.release(line) 减全局/线路计数
        //   - active_by_user[user_id] -= 1
        //   - notify_waiters() 唤醒队列
    }
}
```

RAII：函数返回/panic 时自动 release，不需要 try-finally。

### 4.4 LineHealthRegistry（健康监测）

```rust
// src-tauri/src/line_health.rs 摘要
pub const RING_BUFFER_CAP: usize = 5;
pub const GREEN_MAX_MS: u64 = 150_000;
pub const STALE_AFTER_SECS: u64 = 300;  // 5 min

#[derive(Default)]
pub struct LineHealthRegistry {
    inner: Mutex<HashMap<String, VecDeque<Sample>>>,
}

struct Sample {
    occurred_at: SystemTime,
    latency_ms: u64,
    success: bool,
}

pub enum LineHealthStatus { Green, Yellow, Red, Unknown }

impl LineHealthRegistry {
    /// 每次上游响应完成后调用，记录一条样本
    pub fn record(&self, line: &str, latency_ms: u64, success: bool) {
        let buf = self.inner.lock().entry(line.to_string()).or_default();
        if buf.len() >= RING_BUFFER_CAP { buf.pop_front(); }
        buf.push_back(Sample { occurred_at: SystemTime::now(), latency_ms, success });
    }

    /// 给路由器读快照
    pub fn snapshot(&self) -> LineHealthSnapshot {
        // 对每条线路：
        //   - 缓冲为空 → Unknown
        //   - 最后样本 > STALE_AFTER_SECS → Unknown
        //   - failure_count >= 5 → Red
        //   - median > 150_000ms → Yellow
        //   - 否则 Green
    }
}
```

#### 4.4.1 为什么 Red 只看"全失败"，不看延迟？

历史上曾经把"中位延迟 > 350s"也算作 Red 触发条件。结果：

- 某天上游响应慢一点（如批量推理任务排队），中位延迟达到 360s → Red
- 大量请求被避开 → 上游负载变低 → 响应回到 100s → 但因为没新样本，Red 不下线
- 自锁！

去掉延迟触发条件后：

- 延迟高只影响排序优先级（Yellow），不影响候选资格
- 真正"挂掉"的线路（连续 5 次错误）才会被踢出
- 误伤率显著降低

#### 4.4.2 为什么 STALE_AFTER_SECS 是 5 min

Red 线路通过 stale 转 Unknown 自动恢复：

- 5 min 内：Red → 不被选 → 没有新样本进来
- 5 min 后：状态变 Unknown → 可被选 → 一次"探测"请求
- 成功 → ring 里多 1 个 success → failure_count 降到 4 → Red 解除
- 失败 → ring 里多 1 个 fail（覆盖最老的一个）→ 仍是 Red → 等 5 min 再探测

5 min 是一个权衡：
- 太短（如 1 min）：用户老踩坑（每分钟试一次失败）
- 太长（如 1 h）：上游恢复了但还在排除

### 4.5 自动重试（最外层 handler）

```rust
const GENERATE_IMAGE_MAX_ATTEMPTS: usize = 6;

async fn generate_image(...) -> Result<Response, GatewayError> {
    let user_id = verify_access_token(&headers).await?;
    let mut tried_lines = HashSet::new();
    let mut last_error = None;

    for attempt in 0..6 {
        // 排除已试过的线路
        let permit = acquire_generation_permit(&state, ..., &tried_lines).await?;
        let line = permit.line;
        tried_lines.insert(line.as_str().to_string());

        // 调上游
        let started = Instant::now();
        let result = api::generate_image(...).await;
        let latency_ms = started.elapsed().as_millis() as u64;

        // 记健康
        state.line_health.record(line.as_str(), latency_ms, result.is_ok());

        match result {
            Ok(image) => return Ok(image),
            Err(err) => {
                last_error = Some(err);
                // permit 在这里 drop，自动 release slot
                // 下一次循环重新 acquire，会选不同线路（因为 tried_lines）
            }
        }
    }

    Err(format!("已尝试 {} 条线路均未成功", tried_lines.len()))
}
```

#### 4.5.1 为什么 retry 在网关层而不是客户端

| 客户端 retry | 网关 retry |
|---|---|
| 用户体验：进度条来回跳 | 用户只看见一次"生成中…" |
| 失败后用户得重新点提交 | 自动切线无须用户操作 |
| 每次重试都重新走 queue 排队 | 失败立即释放 slot，重新 acquire 可能秒到 |
| 客户端不知道哪条线路坏了 | 网关精准排除已失败线路 |

#### 4.5.2 实际效果

生产环境某 2.5 小时窗口：
- **44 次重试救回**了原本会让用户看到失败的请求
- 平均 retry 1.4 次，最多 3 次
- 没有出现"6 次都失败"的情况——即使 line4 上游挂了，其他 5 条总能救住

---

## 五、上游适配层：多线路抽象

### 5.1 ImageApiLine 枚举 + Provider 表

```rust
// src-tauri/src/image_provider.rs
pub enum ImageApiLine { Auto, Line1, Line2, Line3, Line4, Line5, Line6 }

pub struct ImageProvider {
    pub api_url: &'static str,
    pub edit_api_url: Option<&'static str>,
    pub model: &'static str,
    pub log_label: &'static str,
    pub user_label: &'static str,
    pub api_key_env_keys: &'static [&'static str],  // 多个候选 env 变量
    pub quality: Option<&'static str>,
    pub format: Option<&'static str>,
    pub reference_image_json_field: ReferenceImageJsonField,
}

pub fn resolve_image_provider(line: ImageApiLine) -> ImageProvider {
    match line {
        ImageApiLine::Line1 => ImageProvider {
            api_url: "https://api3.wlai.vip/v1/images/generations",
            model: "gpt-image-2-all",
            log_label: "image-2:line1",
            api_key_env_keys: &[
                "IMAGE_2_API_KEY",
                "GPT_IMAGE_2_API_KEY",       // 历史兼容
                "WLAI_IMAGE_2_API_KEY",      // 历史兼容
                "NEW_PICTURE_WALL_IMAGE2_API_KEY",  // 历史兼容
            ],
            ...
        },
        // line2-6 同理
    }
}
```

**关键设计**：

1. **不同上游的 API 形态可能不同**——有的是 OpenAI 标准 `/images/generations`，有的是 chat completions 模拟，有的是任务队列 + polling
2. 用枚举做一次抽象，每个具体调用走自己的模块（`yunwu_edit.rs`、`pockgo_chat.rs`、`apimart_task.rs`、`manxiaobai_edit.rs`、`vectorengine_edit.rs`）
3. 共享的部分（HTTP 客户端、错误格式化、参考图下载诊断）抽到 `http_client.rs`、`reference_image.rs`

### 5.2 尺寸映射

不同上游对"尺寸"参数的接受形式不一样（有的要 `1024x1024`，有的要 `1:1` 比例）。统一在 `generation_size_for_line` 做映射：

```rust
pub fn generation_size_for_line(line: &str, size: &str) -> Option<Cow<'a, str>> {
    match line {
        "line5" => match size {
            "1024x1024" => Cow::Borrowed("1:1"),
            "1536x1024" => Cow::Borrowed("3:2"),
            "1792x1024" => Cow::Borrowed("16:9"),
            ...
        },
        "line2" | "line6" => match size {
            "16:9" | "21:9" | "auto" => Cow::Borrowed("1792x768"),
            ...
        },
        ...
    }
}
```

调用流程：

```
前端传 size="1024x1536"
  ↓
gateway 选中 line5
  ↓
generation_size_for_line("line5", "1024x1536") → "3:2"
  ↓
传给 apimart 上游
```

### 5.3 上游 retry 行为

某些上游（line6 manxiaobai）自带 3 次重试，所以在 journal 里能看到 `attempt=1/3`、`attempt=2/3` 这种 inner 重试。这是上游 SDK 层的，不影响网关层的 retry 计数。

```
用户请求
  ↓
网关 attempt 1/6 → 路由到 line6
                    ↓
                    上游 SDK attempt 1/3 → 失败
                    上游 SDK attempt 2/3 → 成功
  ↓
网关收到 200，记 success=true
  ↓
返回用户
```

---

## 六、完整请求生命周期

### 6.1 ASCII 时序图

```
用户点"生成"        前端                  网关                    上游
   │                  │                     │                       │
   │ 点击 [生成]      │                     │                       │
   │─────────────────>│                     │                       │
   │                  │                     │                       │
   │                  │ 检查 globalBusy 是否满 10                    │
   │                  │ 检查 pi.busy 是否当前 slot 在跑              │
   │                  │ 设 pi.product = { status: "queued" }         │
   │                  │                     │                       │
   │                  │ POST /api/generate-image                     │
   │                  │ Bearer <token>      │                       │
   │                  │ { prompt, size, product_images, api_line:auto}│
   │                  │────────────────────>│                       │
   │                  │                     │                       │
   │                  │   1️⃣ verify_access_token → user_id           │
   │                  │   2️⃣ queue.acquire_auto_for_user_excluding   │
   │                  │      ├─ 入队 ticket                          │
   │                  │      ├─ try_acquire_front: 我是不是队首?     │
   │                  │      │   user_limit 内?                       │
   │                  │      ├─ limiter.try_acquire_auto_excluding:  │
   │                  │      │   有 ≥2 个非 line1 候选?               │
   │                  │      │   选最优健康线路                       │
   │                  │      └─ 返回 Permit { line: line5 }           │
   │                  │                     │                       │
   │                  │   3️⃣ api::generate_image(line5)             │
   │                  │                     │ POST apimart            │
   │                  │                     │──────────────────────>│
   │                  │                     │                       │
   │                  │                     │   submit + poll loop  │
   │                  │                     │                       │
   │                  │                     │<──────────────────────│
   │                  │                     │   返回 base64         │
   │                  │   4️⃣ line_health.record(line5, 60s, ok)     │
   │                  │   5️⃣ Permit::drop → release slot            │
   │                  │   6️⃣ 返回 { image, generation_line: line5 }  │
   │                  │<────────────────────│                       │
   │                  │                     │                       │
   │                  │ 渲染图片 + 显示线路                          │
   │                  │ 调 archiveGeneratedImage 把图片传 OSS         │
   │                  │ 调 recordGenerationLog 写 Supabase            │
   │                  │ 更新 pi.product = { status: "succeeded" }     │
   │                  │ 解锁本 slot                                  │
   │<─────────────────│                     │                       │
   │ 看到图           │                     │                       │
```

### 6.2 失败重试时序

```
用户点"生成"        前端                  网关                    上游
   │                  │                     │                       │
   │─────────────────>│ 同上                 │                       │
   │                  │────────────────────>│                       │
   │                  │                     │ acquire → line3       │
   │                  │                     │ api::generate(line3)  │
   │                  │                     │──────────────────────>│
   │                  │                     │              429/timeout
   │                  │                     │<──────────────────────│
   │                  │                     │ record(line3, _, false)│
   │                  │                     │ Permit drop, release  │
   │                  │                     │ tried = {line3}        │
   │                  │                     │                       │
   │                  │                     │ acquire excl=tried    │
   │                  │                     │ → line5               │
   │                  │                     │ api::generate(line5)  │
   │                  │                     │──────────────────────>│
   │                  │                     │             200 OK    │
   │                  │                     │<──────────────────────│
   │                  │                     │ record(line5, 60s, ok)│
   │                  │                     │ 返回 image            │
   │                  │<────────────────────│                       │
   │ 看到图（无感知重试发生过）           │                       │
   │<─────────────────│                     │                       │
```

整个过程客户端只发起 1 次 HTTP 请求，但内部完成了 2 次上游调用 + 1 次路由切换。

### 6.3 队列阻塞场景

假设当前所有用户已经把 user_limit / global_limit 占满：

```
queue: [A1, A2, A3, B1, A4(skip), C1, A5(skip)]
        ↑  ↑  ↑  ↑              ↑
        A 在跑 3 个，A4/A5 暂时跳过
        B 在跑 1 个，B 还有 2 个 user_limit 可用
        C 在跑 0 个

新请求 D1 进来：
  入队 → queue: [A1, A2, A3, B1, A4, C1, A5, D1]
  notify_waiters() 唤醒所有等待者

每个等待者重新评估：
  - A4: active_user_A=3 ≥ 3 → 跳过
  - A5: 同上 → 跳过
  - D1: active_user_D=0 < 3 → 检查全局/线路 → 如果有空位就 acquire

A 释放 1 个槽位：
  active_user_A=2，notify_waiters()
  A4 重新评估：active=2<3 → can_acquire
  按 FIFO（id 排序），A4 比 A5 早，A4 先 acquire
```

---

## 七、配置项总览

### 7.1 服务端 env 变量

```bash
# 全局并发限制
GATEWAY_GENERATION_GLOBAL_LIMIT=21       # 网关进程同时最多跑 21 个

# 每条线路并发限制
GATEWAY_GENERATION_LINE1_LIMIT=1         # wlai，成本高，fallback
GATEWAY_GENERATION_LINE2_LIMIT=4         # yunwu
GATEWAY_GENERATION_LINE3_LIMIT=4         # vectorengine
GATEWAY_GENERATION_LINE4_LIMIT=4         # pockgo
GATEWAY_GENERATION_LINE5_LIMIT=5         # apimart，最稳，多用
GATEWAY_GENERATION_LINE6_LIMIT=4         # manxiaobai

# 每账号并发限制
GATEWAY_GENERATION_USER_LIMIT=3          # 单账号最多 3 个 in-flight

# 网关监听
BACKEND_GATEWAY_HOST=0.0.0.0
BACKEND_GATEWAY_PORT=8787

# 上游 API Keys
IMAGE_2_API_KEY=...
IMAGE_2_LINE2_API_KEY=...
VECTORENGINE_IMAGE_2_API_KEY=...
POCKGO_IMAGE_2_API_KEY=...
APIMART_IMAGE_2_API_KEY=...
MANXIAOBAI_IMAGE_2_API_KEY=...

# Supabase（鉴权）
SUPABASE_URL=...
SUPABASE_ANON_KEY=...
SUPABASE_SERVICE_ROLE_KEY=...
```

注意：`line_limits` 之和**允许超过** `global_limit`。比如这里 1+4+4+4+5+4=22 > 21，留 1 个全局弹性给突发分配——常用最不忙的线路用满，其它线路偶尔补 1 个，不浪费全局容量。

### 7.2 前端 env / 常量

```ts
// useGenerationWorkspace.ts
const FRONTEND_GENERATION_USER_LIMIT = 10;  // 前端软上限
```

```ts
// useLineHealth.ts
const POLL_INTERVAL_MS = 60_000;  // 60s 拉一次线路健康度
```

### 7.3 健康监测常量

```rust
// line_health.rs
pub const RING_BUFFER_CAP: usize = 5;       // 每条线路保留最近 5 条样本
pub const GREEN_MAX_MS: u64 = 150_000;      // 中位 < 150s → Green
pub const STALE_AFTER_SECS: u64 = 300;      // 5 min 无样本转 Unknown
```

### 7.4 限流器常量

```rust
// gateway_limiter.rs
const AUTO_GENERATION_LINES: [&str; 6] = ["line2", "line3", "line4", "line5", "line6", "line1"];
const FALLBACK_LINE: &str = "line1";
const FALLBACK_PROMOTE_THRESHOLD: usize = 2;
```

### 7.5 重试常量

```rust
// backend_gateway.rs
const GENERATE_IMAGE_MAX_ATTEMPTS: usize = 6;  // 网关层最多 6 次重试
```

### 7.6 HTTP 客户端超时

```rust
// http_client.rs
let client = reqwest::Client::builder()
    .timeout(Duration::from_secs(350))    // 单次 HTTP 请求超时 350s
    .build()?;
```

`350s` 是经验值——略小于 Cloudflare 100s 边缘超时的可控放大版本，又大到能容下批量推理高峰。

---

## 八、关键测试覆盖

### 8.1 Rust 单元测试

```
src-tauri/src/gateway_limiter.rs::tests
├── enforces_global_limit_of_twenty_one_active_generations
├── enforces_line_specific_limits
├── release_frees_capacity_for_next_request
├── auto_routing_selects_available_line_and_skips_full_lines
├── auto_routing_excludes_red_health_lines
├── auto_routing_respects_size_compatibility
├── line1_excluded_from_primary_when_two_or_more_other_lines_available
├── line1_excluded_from_primary_even_when_other_lines_busy
├── line1_promoted_when_only_one_other_line_available
├── line1_used_when_all_other_lines_unavailable
├── line1_not_selected_even_when_faster_than_others
└── maps_auto_request_size_to_selected_provider_size

src-tauri/src/gateway_queue.rs::tests
├── waits_for_capacity_in_fifo_order_when_all_lines_are_full
├── returns_immediately_when_no_compatible_line_can_ever_run_request
├── removes_cancelled_waiter_so_later_requests_can_continue
├── queued_auto_request_rechecks_health_before_selecting_line
├── lets_other_users_run_when_front_user_is_at_limit
└── enforces_configured_user_limit

src-tauri/src/line_health.rs::tests
├── empty_is_unknown
├── single_fast_sample_is_green
├── median_in_yellow_range_is_yellow
├── high_latency_with_successes_is_yellow_not_red
├── five_failures_force_red
├── four_failures_with_one_success_is_not_red
└── ring_buffer_caps_at_5
```

总共 85 个 backend-gateway 测试，全部通过。

### 8.2 前端契约测试（TypeScript）

```
tests/generation-concurrency-guard.test.ts
  - 检查前端有 FRONTEND_GENERATION_USER_LIMIT=10
  - 检查每个工作区都传 globalBusy
  - 检查 CapacityNotice 渲染逻辑
  - 检查网关 LINE_LIMIT 默认值（1/4/4/4/5/4）
  - 检查 max attempt 常量存在
  - 检查 acquire_auto_for_user_excluding 接口存在
```

测试用源码字符串断言（grep）而不是运行时行为，简单但能锁定关键不变量。

### 8.3 集成 / 部署验证

构建 release binary 后：
1. `cargo test --bin backend-gateway --no-default-features --release` 跑全部单元测试
2. 在 systemd 服务里 `systemctl restart`，观察 journal 的 `[image-2:line<n>] response_status` 分布
3. 观察 `[gateway] generate_image succeeded on <line> after N retry(ies)` 的频次确认 retry 起作用
4. 观察 line1 占比应该是 ≈0（除非真的有 4+ 条线路坏了）

---

## 九、设计取舍与踩坑记录

### 9.1 为什么 user_limit=3 而不是 5

- 用户体验：3 个并发任务已经足够"流畅"，到 5 反而每张图变慢
- 上游公平：单用户占太多会让其他用户等更久
- 经验值：本系统 7 个运营，全员同时各开 3 个 = 21，正好打满全局

### 9.2 为什么前端 10 而服务端 3

| 层 | 数值 | 作用 |
|---|---|---|
| 前端 | 10 | 软上限，防误触 + 软缓冲 |
| 服务端 user_limit | 3 | 硬上限，强制公平 |
| 服务端 global | 21 | 硬上限，保护进程资源 |

前端 10 不等同"实际并发"——其中 3 个真在跑，7 个在网关队列等。用户视角不会卡死。

### 9.3 为什么 Stale 是 5 min 不是 1 min 或 60 min

历史是 60 min → 自锁问题严重（红的线路 1 小时不复活）；改成 15 min → 仍然偏长；最后定 5 min。

5 min 的意义：上游故障最快 5 min 自动重试一次。
- 如果是闪挂（30s 内恢复），5 min 后探测，几乎肯定成功，Red 解除
- 如果是大故障（数小时），每 5 min 1 次探测的代价是 1 个用户感受一次失败，可接受

### 9.4 为什么 Red 阈值改成"全失败"

历史 3/5 失败 → 5/5 失败：
- 3/5 → 60% 失败率才算挂。但生产上经常出现 4/5 = 80% 失败率仍然在跑（少量上游退化但没死），改严之后**误判率显著下降**
- 老规则把"延迟过高"也算 Red，结果**自锁**死循环（红 → 不路由 → 没新样本 → 永远红）；新规则只看失败次数，加上 Stale 5 min，永远不会自锁

### 9.5 为什么需要 Fallback Line

如果不区分 line1 是 fallback，排序算法会让 line1 经常胜出（active=0, latency 低）。生产数据：旧版 line1 占 17%，但 line1 上游成本是其它的 3-5 倍——每月白白多付几千块钱。

修复后 line1 占比从 17.3% → 0%。

### 9.6 为什么 retry 在网关而不是客户端

- 客户端不知道哪条线路坏了，下次 retry 还可能踩坑
- 重新走 queue 排队、重新建 HTTPS 连接、重新走 OSS 上传参考图 = 几秒到几十秒额外开销
- 网关层在同一个请求 context 里 retry，连参考图都不用重新传

### 9.7 队列里"跳过"队首是不是违反 FIFO？

技术上是。但**用户层面不感知**：
- 同一个用户的 ticket 还是 FIFO（A1 比 A4 早 acquire）
- 跨用户才有"跳过"：A 占满 3 个时 B 不需要排在 A 后面
- 这是 throughput-fair-fifo，不是 strict-fifo

### 9.8 为什么不直接用 tokio Semaphore + AsyncMutex

`tokio::sync::Semaphore` 缺乏：
- 多级限制（全局 + 线路 + 用户）联合判定
- 健康感知的"哪条线路应该被选"
- "队首用户已满则跳到下一个" 公平性

我们的 `GatewayGenerationQueue` 用 `std::sync::Mutex<QueueState> + tokio::sync::Notify` 实现：
- 状态变更是同步的（Mutex 临界区短）
- 等待是异步的（`notify.notified().await`）
- 不需要 `tokio::Mutex` 因为我们持锁时间很短，不跨 await 点

### 9.9 为什么前端永远 api_line="auto"

历史上前端能让用户挑线路：
- 用户挑了 line2 → 网关接到 api_line=line2 → 强制走 line2 → line2 挂了用户看到错误
- 用户不懂哪条好，全靠运营经验
- 服务端没法集中调度

改成统一 auto 后：
- 用户视角：永远是"生成中"→"成功/失败"，简单
- 服务端可以根据 health 实时调整
- UI 上的"线路选择器"改成"显示用了哪条"，仅作信息

---

## 十、如何把这套框架移植到新项目

### 10.1 评估你是否需要这套框架

**适合**：
- 多个互为备份的上游服务（≥3 个）
- 上游响应时间长（10s 以上）
- 上游有并发限制 或 偶发故障
- 需要平摊成本（高/低端服务混用）
- 多用户、需要防止单用户挤兑

**不适合**：
- 单上游 / 上游稳定且便宜
- 短响应时间（< 1s）→ 直接 HTTP client + retry-with-backoff 就够
- 单用户场景

### 10.2 移植步骤

#### 第 1 步：抽象上游

定义你的 `Line` enum 和 `Provider` 表：

```rust
pub enum ApiLine { Auto, Line1, Line2, ... }

pub struct Provider {
    pub api_url: &'static str,
    pub api_key_env_keys: &'static [&'static str],
    pub model: &'static str,
    pub log_label: &'static str,
    // 你需要的其它差异化字段
}

pub fn resolve_provider(line: ApiLine) -> Provider { match line { ... } }
```

每条线路的具体调用逻辑（参数构造、响应解析）各自一个 module。

#### 第 2 步：定义健康监测

直接抄 `line_health.rs`，调整：

```rust
pub const RING_BUFFER_CAP: usize = 5;        // 你想要的样本数
pub const GREEN_MAX_MS: u64 = 150_000;       // 你的"快"阈值
pub const STALE_AFTER_SECS: u64 = 300;       // 你的"复活"时间
```

```rust
let status = if failure_count >= RING_BUFFER_CAP { Red }
             else if median >= GREEN_MAX_MS { Yellow }
             else { Green };
```

#### 第 3 步：定义限流器

抄 `gateway_limiter.rs`，三个关键自定义点：

1. `AUTO_GENERATION_LINES` 顺序：把你想"优先用"的线路放前面
2. `FALLBACK_LINE`：如果你有一条"贵线路"想兜底，设它；如果没有，删掉 fallback 两级选择，回归单级
3. `FALLBACK_PROMOTE_THRESHOLD`：兜底激活阈值，常用 2

```rust
const AUTO_GENERATION_LINES: [&str; N] = ["...", "...", ...];
const FALLBACK_LINE: &str = "...";        // 可选
const FALLBACK_PROMOTE_THRESHOLD: usize = 2;
```

排序键基本不用动：`(active, health_rank, latency, order)`。

#### 第 4 步：定义队列

抄 `gateway_queue.rs`，核心数据结构：

```rust
struct QueueState {
    limiter: YourLimiter,
    user_limit: usize,
    active_by_user: HashMap<UserId, usize>,
    next_ticket: u64,
    waiting: VecDeque<QueueTicket>,
}

enum QueueRequest {
    Auto { context: YourContext, exclude: HashSet<String> },
    Line { line: String },
}
```

acquire 流程基本不用改。

#### 第 5 步：定义最外层 handler

```rust
const MAX_ATTEMPTS: usize = N;

async fn handle_request(state, headers, body) -> Result<Response, Error> {
    let user_id = verify_auth(headers).await?;
    let mut tried = HashSet::new();
    let mut last_error = None;

    for attempt in 0..MAX_ATTEMPTS {
        let permit = match state.queue.acquire_excluding(&user_id, ..., &tried).await {
            Ok(p) => p,
            Err(_) if attempt > 0 => break,
            Err(e) => return Err(e.into()),
        };
        let line = permit.line.clone();
        tried.insert(line.clone());

        let started = Instant::now();
        let result = call_upstream(&line, &body).await;
        let latency_ms = started.elapsed().as_millis() as u64;
        state.health.record(&line, latency_ms, result.is_ok());

        match result {
            Ok(resp) => return Ok(resp),
            Err(e) => { last_error = Some(e); continue; }
        }
    }

    Err(last_error.unwrap_or_default())
}
```

#### 第 6 步：定义前端层（如适用）

- N 个 slot × N 个工作区，每个 slot 独立维护 busy
- 账号级软上限，远比服务端硬上限大（10 vs 3 这种比例）
- 始终发 `api_line="auto"`

#### 第 7 步：测试与监控

必须有的测试：

```
- 全局 limit 拒绝
- 每线路 limit 拒绝
- 释放后能再 acquire
- Auto 路由选最优
- Red 线路被排除
- Fallback line 只在阈值下激活
- 队列 FIFO + 用户公平
- 健康环形缓冲：N 失败 → Red、Stale → Unknown
- Retry 排除已试线路
```

监控数据点：
- 每条线路 image_count（入口）/ response_status=200（成功）/ 4xx/5xx
- retry 重试救回次数 vs attempt 失败总数
- 每用户成功数
- gateway 自身 429（队列拒绝，应该 = 0）

### 10.3 不要照抄的部分

| 这套框架的细节 | 你可能需要改的 |
|---|---|
| GATEWAY_GENERATION_GLOBAL_LIMIT=21 | 按你的进程能撑多少并发改 |
| LINE5=5 | 按上游的实际并发能力改 |
| user_limit=3 | 按你想给每用户多少配额改 |
| STALE_AFTER_SECS=300 | 按上游故障平均恢复时间改 |
| GENERATE_IMAGE_MAX_ATTEMPTS=6 | 按你有几条线路（一般 = 线路数）改 |
| reqwest 350s 超时 | 按上游响应时间分布改 |
| RING_BUFFER_CAP=5 | 想更敏感就调小，想更稳就调大 |
| FALLBACK_PROMOTE_THRESHOLD=2 | 想 line1 更晚介入就调高（如 1） |

### 10.4 部署清单

**Linux + systemd**：

```
/opt/<your-app>/
├── bin/
│   ├── backend-gateway              # 二进制
│   └── backend-gateway.bak-<date>   # 备份
└── secrets/
    └── gateway.env                  # 所有 env 变量

/etc/systemd/system/<your-app>-gateway.service:
  [Service]
  EnvironmentFile=/opt/<your-app>/secrets/gateway.env
  ExecStart=/opt/<your-app>/bin/backend-gateway
  Restart=on-failure
  User=<service-user>
```

**部署流程**（无停机替换）：

```bash
# 备份
sudo cp /opt/<your-app>/bin/backend-gateway /opt/<your-app>/bin/backend-gateway.bak-$(date +%Y%m%d-%H%M)

# Linux 不允许直接覆盖正在运行的可执行文件 → rm + cp 走 inode 替换
sudo rm /opt/<your-app>/bin/backend-gateway
sudo cp <new-binary> /opt/<your-app>/bin/backend-gateway
sudo chmod 755 /opt/<your-app>/bin/backend-gateway
sudo chown <service-user>:<service-user> /opt/<your-app>/bin/backend-gateway

# 重启
sudo systemctl restart <your-app>-gateway

# 验证
systemctl is-active <your-app>-gateway
curl -sS -o /dev/null -w "%{http_code}\n" http://localhost:8787/health
sudo journalctl -u <your-app>-gateway --since "30 seconds ago" --no-pager | tail
```

**回退预案**：

```bash
sudo rm /opt/<your-app>/bin/backend-gateway
sudo cp /opt/<your-app>/bin/backend-gateway.bak-<date> /opt/<your-app>/bin/backend-gateway
sudo systemctl restart <your-app>-gateway
```

### 10.5 可观察性建议

journal 里至少打这几类日志（带 `[image-2:line<n>]` 这种 tag 方便 grep 聚合）：

```
[image-2:line5-apimart] image_count=2 image_refs=[url:259,url:282] prompt_chars=523 size=1536x1024
[image-2:line5-apimart] response_status=200 OK response_preview={...}
[gateway] generate_image attempt 1/6 failed on line4: <错误描述>
[gateway] generate_image succeeded on line5 after 1 retry(ies)
```

定期 grep 出来按线路 / 状态聚合，看分布是否健康。

---

## 附录 A：本系统的真实代码位置

| 概念 | 文件 |
|---|---|
| 限流器 | `src-tauri/src/gateway_limiter.rs` |
| FIFO 队列 | `src-tauri/src/gateway_queue.rs` |
| 健康监测 | `src-tauri/src/line_health.rs` |
| 上游枚举 + provider 表 | `src-tauri/src/image_provider.rs` |
| 网关 HTTP handler | `src-tauri/src/bin/backend_gateway.rs` |
| 业务请求入口 + retry | `src-tauri/src/bin/backend_gateway.rs::generate_image` |
| 上游具体调用 | `src-tauri/src/api.rs` + 各线路模块（`yunwu_edit.rs`、`pockgo_chat.rs`、`apimart.rs`、`manxiaobai_edit.rs`、`vectorengine_edit.rs`） |
| 前端账号软上限 | `src/hooks/useGenerationWorkspace.ts` |
| 前端工作区调度 | `src/components/WorkspacePages.tsx` |
| 前端 slot 实例化 | `src/hooks/useGenerationWorkspace.ts` |
| 前端调用入口 | `src/lib/tauri.ts::generateImageWithLine` |
| 健康度拉取 | `src/hooks/useLineHealth.ts` |

## 附录 B：调度时序速查表

| 事件 | 触发 | 操作 |
|---|---|---|
| 用户点提交 | 前端按钮 | 检查 `pi.busy` + `globalBusy`，设 `pi.product.status="queued"` |
| 进入队列 | `acquire_auto_for_user` | 入 `waiting` 队尾，分配 ticket_id |
| 唤醒检查 | `notify.notified().await` 返回 | 遍历 waiting，找第一个 can_acquire 的 ticket |
| 拿到 permit | limiter.try_acquire_auto_excluding 返回 Some | 更新 active_global、active_by_line、active_by_user |
| 调上游 | api::generate_image | reqwest POST 上游，等响应 |
| 上游完成 | response 或 timeout | `health.record(line, latency, success)` |
| 失败 retry | result.is_err() | `tried.insert(line)`，下一次循环重新 acquire |
| 成功返回 | result.is_ok() | 返回 Response，Permit drop |
| Permit drop | RAII | release 减计数，`notify_waiters()` |
| 等待者醒来 | notify | 各自重新评估 can_acquire |

---

**文档版本**：v1.0（2026-05-21）  
**对应代码**：呈尚策划美工生图系统 PRO 2.0.0  
**网关 binary hash**：`4c0a1a57543aebabe60d1f53a51d1495a5caad1bb885cde30e0ceea56a69a91e`
